% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/questions_outcome_model.R
\name{fit_spillover_screen}
\alias{fit_spillover_screen}
\title{Screen candidate module x timepoint combinations from a long-format object}
\usage{
fit_spillover_screen(
  long,
  candidates,
  covariate_cols = NULL,
  family = stats::gaussian(),
  n_perm = 5000,
  n_boot = 5000,
  cv_k = 5,
  cv_reps = 50,
  conf = 0.95,
  seed = NULL,
  n_family = NULL
)
}
\arguments{
\item{long}{Long data.frame from prepare_spillover_terms() (needs columns
module, timepoint, y, direct, spill, and any covariate columns).}

\item{candidates}{data.frame(module, timepoint) from select_candidates().}

\item{covariate_cols}{Optional character vector of column names in \code{long} to
pass as covariates to the outcome model. Default NULL (none).}

\item{family, n_perm, n_boot, cv_k, cv_reps, conf}{Passed to fit_spillover_outcome().}

\item{seed}{Optional integer seed applied once before the screen.}

\item{n_family}{Optional integer overriding the BH family size (default = number
of candidates actually fit). Set to the pre-specified family size if some
candidates are dropped for insufficient data but should still count.}
}
\value{
data.frame sorted by p_BH: module, timepoint, n, beta_spill, ci_lo,
ci_hi, sign_stab, p_perm, p_BH, cv_dR2, p_direct, loo_max_p.
}
\description{
Consumes the output of prepare_spillover_terms() directly. For each candidate
(module, timepoint) it selects that timepoint's rows for the module, fits the
covariate-aware outcome model, and Benjamini--Hochberg adjusts the permutation
p-values across the WHOLE candidate family (the reported FDR denominator).
}
\seealso{
\code{\link{prepare_spillover_terms}}, \code{\link{select_candidates}}
}
