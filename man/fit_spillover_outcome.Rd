% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/questions_outcome_model.R
\name{fit_spillover_outcome}
\alias{fit_spillover_outcome}
\title{Fit the direct-plus-spillover outcome model for one module and validate it}
\usage{
fit_spillover_outcome(
  y,
  direct,
  spill,
  covariates = NULL,
  family = stats::gaussian(),
  n_perm = 5000,
  n_boot = 5000,
  cv_k = 5,
  cv_reps = 50,
  conf = 0.95,
  seed = NULL
)
}
\arguments{
\item{y}{Numeric outcome (one value per unit/row).}

\item{direct, spill}{Numeric vectors, same length/order as y.}

\item{covariates}{Optional numeric matrix/data.frame of additional predictors
(rows = units, same order as y). Included in the full model, the reduced
(permutation/CV baseline) model, the bootstrap, and CV. Default NULL.}

\item{family}{GLM family (default gaussian()); permutation/CV assume identity-link LM.}

\item{n_perm, n_boot}{Permutation and bootstrap counts (defaults 5000).}

\item{cv_k, cv_reps}{CV folds and repeats (defaults 5, 50).}

\item{conf}{Bootstrap CI level (default 0.95).}

\item{seed}{Optional integer seed.}
}
\value{
One-row data.frame: n, beta_spill, ci_lo, ci_hi, sign_stab, p_perm,
cv_dR2, p_direct, loo_max_p.
}
\description{
Fits \eqn{y = \beta_0 + \beta_{dir}\,\mathrm{direct} + \beta_{spill}\,\mathrm{spill}
+ \gamma^\top Z + \varepsilon} with direct and spillover standardized, and
evaluates the partial spillover coefficient with the small-sample battery:
a Freedman--Lane permutation test (permuting residuals of the reduced model,
which contains direct AND any covariates, so the test conditions on both),
a case bootstrap for sign-stability and a CI, repeated k-fold CV for the added
out-of-sample R^2 over a direct+covariates baseline, and leave-one-out.
}
\seealso{
\code{\link{fit_spillover_screen}}, \code{\link{prepare_spillover_terms}}
}
