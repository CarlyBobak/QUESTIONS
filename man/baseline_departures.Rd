% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/questions_prepare_terms.R
\name{baseline_departures}
\alias{baseline_departures}
\title{Per-sample departures from each unit's own baseline}
\usage{
baseline_departures(expr, sample_unit, baseline)
}
\arguments{
\item{expr}{Gene x sample expression matrix (rownames = genes, colnames = sample IDs).}

\item{sample_unit}{Character vector (length = ncol(expr)) giving each sample's unit/animal ID.}

\item{baseline}{Logical vector (length = ncol(expr)) TRUE for baseline samples.}
}
\value{
A gene x sample matrix of departures (same dim/dimnames as expr).
}
\description{
Computes \code{D = expr - baseline[, unit]}, where each unit's baseline is its mean
expression over the samples flagged as baseline (e.g. pre-infection timepoints).
}
\seealso{
\code{\link{prepare_spillover_terms}}
}
