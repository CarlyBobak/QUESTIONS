% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/questions_prepare_terms.R
\name{descriptive_module_spillover}
\alias{descriptive_module_spillover}
\title{Descriptive module-level spillover matrix (candidate-ranking statistic)}
\usage{
descriptive_module_spillover(network, membership, effects, min_size = 10)
}
\arguments{
\item{network}{Adjacency matrix or WGCNA-style object (row-normalized internally).}

\item{membership}{Named gene -> module vector. May be integer ids or "M"-style
labels; rownames of the returned matrix are always "M\if{html}{\out{<k>}}".}

\item{effects}{Named list of per-timepoint gene effect vectors
(e.g. from baseline_effect_vectors()).}

\item{min_size}{Minimum module size to summarize (default 10).}
}
\value{
A modules x timepoints matrix (rownames "M\if{html}{\out{<k>}}") of mean module spillover.
}
\description{
Propagates each per-timepoint effect vector over the FIXED consensus network
with calculate_spillover() and averages gene-level spillover within each module
(restricted to modules of at least \code{min_size} genes). Returns the modules x
timepoints matrix that select_candidates() ranks on. This replaces the earlier
degenerate single-network use of spillover_timecourse() with a direct,
tested calculate_spillover() call; it reproduces spillover_results_baseline.rds
$module_spillover.
}
\seealso{
\code{\link{baseline_effect_vectors}}, \code{\link{select_candidates}}
}
