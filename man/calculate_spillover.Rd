% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/questions.R
\name{calculate_spillover}
\alias{calculate_spillover}
\title{Calculate gene-level spillover (neighbor-weighted exposure)}
\usage{
calculate_spillover(
  network,
  initiator_genes,
  gene_effects = NULL,
  row_normalize = TRUE,
  prune = FALSE,
  prune_args = list(signif_level = 0.05)
)
}
\arguments{
\item{network}{An adjacency matrix, TOM matrix, or WGCNA-style result object.}

\item{initiator_genes}{Genes to initialize spillover from (used if gene_effects is NULL).}

\item{gene_effects}{Optional named numeric vector of initiator effects (e.g. logFC).}

\item{row_normalize}{Logical. Row-normalize W to row-stochastic before propagating (default TRUE).}

\item{prune}{Logical. Apply the disparity backbone before propagating (default FALSE).}

\item{prune_args}{Named list passed to prune_matrix(), e.g. list(signif_level = 0.05).}
}
\value{
A named numeric vector of spillover scores over all genes in the network.
}
\description{
Propagates a set of initiator effects one step over a gene similarity network as a
neighbor-weighted exposure: spillover = W \%*\% effects, with W optionally row-normalized
to a row-stochastic transition matrix. This is the exposure-mapping quantity shared with
the ANSWERS peer-exposure construction. There is no damping parameter: sparsification,
not a tuned alpha, controls locality.
}
\seealso{
\code{\link{prune_matrix}}
}
